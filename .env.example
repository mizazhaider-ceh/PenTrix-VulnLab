###############################################################################
#  PenTrix Vuln Lab — Environment Configuration
#  Copy this file to .env and adjust values as needed.
#  Default values work out of the box for local development.
###############################################################################

# ── Web Application ──────────────────────────────────────
PORT=5000                                  # Host port for web app
SECRET_KEY=super_secret_key_12345          # Flask session secret (intentionally weak)
API_KEY=sk-pentrix-internal-key-9876       # Internal API key
FLAG_SECRET=pentrix_lab_secret_2024        # HMAC secret for flag generation

# ── Internal Service ─────────────────────────────────────
INTERNAL_SERVICE_URL=http://internal:8080  # Backend microservice URL

# ── Redis ────────────────────────────────────────────────
REDIS_URL=redis://redis:6379/0            # Redis connection (no auth — intentional)

# ── Linux CTF Machine ────────────────────────────────────
LINUX_HTTP_PORT=8888                       # Host port for Linux CTF web (Apache)
LINUX_SSH_PORT=2222                        # Host port for Linux CTF SSH

# ── Database ─────────────────────────────────────────────
DATABASE_URL=sqlite:////app/data/pentrix.db
